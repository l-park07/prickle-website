// Firebase Web app config for getprickle.app.
// This is NOT a secret — it identifies the project to Firebase; access
// control comes from Firestore Security Rules + App Check, not from hiding
// this object. Fill in with the config from:
// Firebase console > Project settings > Your apps > Web app.
//
// If you haven't registered a Web app for this project yet, do that first
// (console > Add app > Web), then paste its config below.
export const firebaseConfig = {
  apiKey: 'TODO_API_KEY',
  authDomain: 'TODO_PROJECT_ID.firebaseapp.com',
  projectId: 'TODO_PROJECT_ID',
  storageBucket: 'TODO_PROJECT_ID.appspot.com',
  messagingSenderId: 'TODO_SENDER_ID',
  appId: 'TODO_APP_ID',
};
